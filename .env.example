# --- Local development ---
MONGODB_URI=mongodb+srv://where_is_my_bmtc:where_is_my_bmtc@where-is-my-bmtc-backen.oyi65ir.mongodb.net/?appName=Where-is-My-BMTC-backend
MONGODB_DB=bmtc


REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=true

ENVIRONMENT=development
DEBUG=false
API_PREFIX=/api
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=*
RATE_LIMIT_PER_MINUTE=60

SIMULATION_INTERVAL_SECONDS=3.0
SIMULATION_STEPS_BETWEEN_STOPS=10

# --- Render (set in Dashboard → Environment, not in repo) ---
# RENDER=true                    # set automatically by Render
# PORT=10000                     # set automatically by Render
# MONGODB_URI=mongodb+srv://...  # MongoDB Atlas connection string
# CORS_ORIGINS=https://your-app.onrender.com,https://your-flutter-app.com
# REDIS_ENABLED=false            # default on Render unless you add Redis
# REDIS_URL=redis://...          # from Render Redis add-on if used
